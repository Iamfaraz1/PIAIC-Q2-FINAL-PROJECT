/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com'], // Add 'fakestoreapi.com' to the domains
      },
};

export default nextConfig;
